<section>
<div class="pt-5 pl-5">
<strong><%= @user.name %></strong>
<%= cover_image(@user) %>
</div>
<div class="pl-5">
<% @user.tweets.each do |tweet| %>
<%= tweet.tweet %>
<% end %>
</div>
</section>